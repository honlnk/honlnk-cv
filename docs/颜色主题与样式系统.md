# 颜色主题与样式系统文档

## 📋 概述

本文档详细描述了个人简历网站的颜色主题、样式系统和设计规范。项目采用 UnoCSS + CSS 变量的混合架构，既保证了原子化 CSS 的便利性，又提供了灵活的主题切换能力。

---

## 🎨 颜色系统

### 主色调

| 变量名 | RGB 值 | 十六进制 | 用途 | 说明 |
|--------|---------|----------|------|------|
| `--primary` | `44 62 80` | `#2c3e50` | 主要文字、标题 | 深蓝灰色，沉稳专业 |
| `--secondary` | `52 152 219` | `#3498db` | 链接、按钮、强调 | 蓝色，科技感 |
| `--accent` | `52 73 94` | `#34495e` | 次要强调色 | 深灰色，辅助色 |
| `--success` | `39 174 96` | `#27ae60` | 成功状态 | 绿色，积极向上 |
| `--warning` | `243 156 18` | `#f39c12` | 警告提示 | 橙色，醒目 |
| `--error` | `231 76 60` | `#e74c3c` | 错误状态 | 红色，警示 |

### 背景色系

| 变量名 | RGB 值 | 十六进制 | 用途 |
|--------|---------|----------|------|
| `--section-bg` | `248 249 250` | `#f8f9fa` | 区块背景 |
| `--header-bg` | `248 249 250` | `#f8f9fa` | 头部背景 |

### 文字色系

| 变量名 | RGB 值 | 十六进制 | 用途 |
|--------|---------|----------|------|
| `--text-primary` | `44 62 80` | `#2c3e50` | 主要文字 |
| `--text-secondary` | `127 140 141` | `#7f8c8d` | 次要文字 |

### 暗色模式（预留）

| 变量名 | RGB 值 | 十六进制 | 说明 |
|--------|---------|----------|------|
| `--bg-primary-dark` | `26 26 26` | `#1a1a1a` | 暗色主背景 |
| `--bg-secondary-dark` | `45 45 45` | `#2d2d2d` | 暗色次要背景 |
| `--text-primary-dark` | `255 255 255` | `#ffffff` | 暗色主文字 |
| `--text-secondary-dark` | `160 160 160` | `#a0a0a0` | 暗色次文字 |

---

## 🎭 设计系统

### 排版规范

#### 字体族
- **无衬线字体**：`Inter`, `system-ui`, `sans-serif`
- **等宽字体**：`JetBrains Mono`, `monospace`

#### 字体大小层级
| 元素 | 大小 | 行高 | 用途 |
|------|------|------|------|
| `h1` | `2.25rem` (36px) | 1.2 | 主标题 |
| `h2` | `1.875rem` (30px) | 1.2 | 二级标题 |
| `h3` | `1.5rem` (24px) | 1.2 | 三级标题 |
| `h4` | `1.25rem` (20px) | 1.2 | 四级标题 |
| `body` | `1rem` (16px) | 1.7 | 正文 |

#### 字重规范
- **常规**：`400` (normal)
- **中等**：`500` (medium)
- **半粗**：`600` (semibold)
- **粗体**：`700` (bold)

### 间距系统

#### 基础间距单位
- **基础单位**：`0.25rem` (4px)
- **常用间距**：
  - `xs`: `0.25rem` (4px)
  - `sm`: `0.5rem` (8px)
  - `md`: `1rem` (16px)
  - `lg`: `1.5rem` (24px)
  - `xl`: `2rem` (32px)

#### 组件间距
- **卡片内边距**：`1.5rem` (24px)
- **区块间距**：`2rem` (32px)
- **元素间距**：`1rem` (16px)

### 圆角系统

| 类名 | 大小 | 用途 |
|------|------|------|
| `rounded-sm` | `0.125rem` (2px) | 小元素 |
| `rounded` | `0.25rem` (4px) | 按钮、输入框 |
| `rounded-md` | `0.375rem` (6px) | 中等组件 |
| `rounded-lg` | `0.5rem` (8px) | 卡片 |
| `rounded-xl` | `0.75rem` (12px) | 大卡片 |
| `rounded-2xl` | `1rem` (16px) | 特殊元素 |

### 阴影系统

| 类名 | 描述 | 使用场景 |
|------|------|----------|
| `shadow-sm` | 轻微阴影 | 悬浮状态 |
| `shadow-card` | 卡片阴影 | 卡片组件 |
| `shadow-card-hover` | 卡片悬浮阴影 | 卡片悬浮状态 |
| `shadow-lg` | 大阴影 | 模态框、弹层 |

---

## 🛠 UnoCSS 配置

### 预设配置
```typescript
import {
  defineConfig,
  transformerDirectives,
} from 'unocss'
import presetWind3 from '@unocss/preset-wind3'
import presetIcons from '@unocss/preset-icons'

export default defineConfig({
  presets: [
    presetWind3(),    // 最新推荐预设 (替代已弃用的 presetUno)
    presetIcons({     // 图标预设
      scale: 1.2,
      warn: true,
    }),
  ],
  transformers: [
    transformerDirectives(), // 支持指令语法
  ],
})
```

> **注意**：`presetUno()` 和 `presetWind()` 已在 UnoCSS v0.66+ 中弃用，推荐使用 `presetWind3()`

### 快捷类定义

| 类名 | 定义 | 用途 |
|------|------|------|
| `card-base` | 基础卡片样式 | 卡片基础样式 |
| `card-hover` | 卡片悬浮效果 | 卡片悬浮状态 |
| `section-title` | 区块标题样式 | 各区块标题 |
| `btn-primary` | 主要按钮样式 | 主要操作按钮 |
| `text-gradient` | 渐变文字 | 特殊文字效果 |
| `glass-effect` | 玻璃态效果 | 现代化卡片 |
| `hover-lift` | 悬浮上升效果 | 交互反馈 |

### 自定义规则

#### 文字闪光效果
```css
/* UnoCSS 自定义规则 */
['text-shimmer', {
  'background': 'linear-gradient(90deg, currentColor 0%, rgba(255,255,255,0.3) 50%, currentColor 100%)',
  'background-size': '200% auto',
  'background-clip': 'text',
  '-webkit-background-clip': 'text',
  'color': 'transparent',
}]
/* 使用时配合自定义 CSS 动画或 UnoCSS 内置动画类 */
```

#### 鼠标跟随效果
```css
/* UnoCSS 自定义规则 */
['mouse-glow', {
  'position': 'fixed',
  'pointer-events': 'none',
  'z-index': '9999',
  'width': '20px',
  'height': '20px',
  'background': 'radial-gradient(circle, rgba(52,152,219,0.3) 0%, transparent 70%)',
  'border-radius': '50%',
  'transform': 'translate(-50%, -50%)',
  'transition': 'all 0.1s ease-out',
}]
```

---

## 🎭 动画系统

### 动画使用说明

在 UnoCSS v0.66+ 中，动画系统已简化，推荐使用以下方式：

1. **内置动画类**：直接使用 UnoCSS 提供的动画类（如 `animate-pulse`）
2. **自定义 CSS 动画**：在 CSS 文件中定义 `@keyframes`，然后通过 `animate-[动画名称]` 类使用

### 动画时长

| 类型 | 时长 | 缓动函数 |
|------|------|----------|
| **快速** | `0.15s` | `ease-out` |
| **标准** | `0.3s` | `cubic-bezier(0.4, 0, 0.2, 1)` |
| **缓慢** | `0.5s` | `ease-in-out` |
| **页面过渡** | `0.8s` | `cubic-bezier(0.4, 0, 0.2, 1)` |

### 内置动画示例

```css
/* 常用内置动画类 */
animate-pulse          /* 脉冲效果 */
animate-bounce         /* 弹跳效果 */
animate-spin           /* 旋转效果 */
animate-ping           /* 波纹效果 */
```

### 自定义动画实现

#### 1. 闪光动画 (Shimmer)
**方法一：CSS 文件中定义**
```css
@keyframes shimmer {
  0% { background-position: 0% center; }
  100% { background-position: 200% center; }
}

/* 使用方式 */
.text-shimmer {
  animation: shimmer 3s linear infinite;
}
```

**方法二：配合 UnoCSS 类**
```html
<div class="text-shimmer animate-[shimmer_3s_linear_infinite]">
  标题文字
</div>
```

#### 2. 浮动动画 (Float)
```css
@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

/* 使用方式 */
.float-element {
  animation: float 3s ease-in-out infinite;
}
```

#### 3. 慢速脉冲动画
```css
@keyframes pulse-slow {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

/* 使用方式 */
.pulse-slow {
  animation: pulse-slow 3s ease-in-out infinite;
}
```

#### 4. 滚动显示 (Scroll Reveal)
```css
.scroll-reveal {
  opacity: 0;
  transform: translateY(30px);
  transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}

.scroll-reveal.active {
  opacity: 1;
  transform: translateY(0);
}
```
- **用途**：页面滚动时的内容显示
- **触发**：滚动到视窗内

---

## 🎨 组件样式规范

### 头部组件 (Header)
- **背景**：渐变背景 `header-gradient`
- **标题**：`text-4xl` + `text-primary` + `font-semibold`
- **联系方式**：`contact-link` 类，蓝色主题

### 卡片组件
- **基础样式**：`card-base`
- **悬浮效果**：`card-hover` 或 `hover-lift`
- **玻璃态**：`glass-card` (用于现代化设计)

### 按钮样式
- **主要按钮**：`btn-primary`
- **悬浮状态**：`hover:bg-secondary/90`
- **焦点状态**：`focus:ring-2 focus:ring-secondary/50`

### 链接样式
- **颜色**：`--secondary` (蓝色)
- **悬浮**：`--secondary/80` (80% 透明度)
- **过渡**：`transition-colors duration-200`

### 标签样式 (Tech Tags)
- **基础**：`tech-tag` 类
- **背景**：`rgba(var(--secondary), 0.1)`
- **圆角**：`rounded-full`
- **间距**：`mr-2 mb-2`

---

## 🌙 暗色模式支持

### 实现方式
项目使用 CSS 变量系统，为暗色模式预留了完整的变量定义：

```css
@media (prefers-color-scheme: dark) {
  :root {
    /* 暗色模式变量会自动替换亮色模式变量 */
  }
}
```

### 暗色模式适配
- **玻璃态卡片**：已适配暗色模式
- **文字颜色**：使用 CSS 变量自动切换
- **背景颜色**：预留完整的暗色背景变量

---

## 📱 响应式设计

### 断点系统
使用 UnoCSS 标准断点：
- **sm**: `640px` - 小屏幕
- **md**: `768px` - 平板
- **lg**: `1024px` - 桌面
- **xl`: `1280px` - 大屏幕
- **2xl`: `1536px` - 超大屏幕

### 响应式示例
```css
/* 卡片响应式内边距 */
.card {
  @apply p-4 sm:p-6 md:p-8 lg:p-6;
}

/* 网格响应式 */
.project-grid {
  @apply grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6;
}
```

---

## 🔧 样式架构

### 文件结构
```
src/assets/styles/
├── main.css          # 主样式文件
├── variables.css     # CSS 变量定义
└── uno.css          # UnoCSS 生成的样式（自动）
```

### 分层架构
```css
@layer base {
  /* 基础元素样式 */
}

@layer components {
  /* 组件样式 */
}

@layer utilities {
  /* 工具类样式 */
}
```

### 样式策略
- **基础样式**：使用原生 CSS + CSS 变量
- **组件样式**：混合使用原生 CSS 和 UnoCSS
- **原子化样式**：使用 UnoCSS 快捷类
- **动画效果**：使用原生 CSS 关键帧

---

## 🎯 使用指南

### 1. 颜色使用
```css
/* 推荐使用 CSS 变量 */
color: rgb(var(--primary));           /* 主色 */
background-color: rgb(var(--section-bg)); /* 背景色 */
border-color: rgba(var(--secondary), 0.2); /* 透明边框 */
```

### 2. 间距使用
```css
/* 优先使用 UnoCSS 类名 */
<div class="p-6 mb-4">  /* 内边距 1.5rem，外边距 1rem */
```

### 3. 动画使用
```css
/* 滚动显示动画 */
<div class="scroll-reveal">内容</div>

/* 悬浮效果 */
<div class="hover-lift">卡片</div>
```

### 4. 响应式使用
```css
/* 响应式网格 */
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
```

---

## 📝 开发注意事项

### 1. 颜色使用
- 优先使用 CSS 变量而不是硬编码颜色值
- 使用 `rgb()` 函数以支持透明度
- 保持足够的对比度以确保可读性

### 2. 性能优化
- 避免过度使用 `@apply`，已在优化中减少 83% 的使用
- 优先使用 UnoCSS 原子类而不是自定义样式
- 合理使用动画，避免性能问题

### 3. UnoCSS 版本兼容性
- **当前版本**：UnoCSS v0.66+
- **弃用变更**：
  - `presetUno()` 和 `presetWind()` 已弃用
  - 推荐使用 `presetWind3()` 替代
  - 动画配置方式已简化，不再支持 `animations` 和 `keyframes` 配置
- **导入方式更新**：
  ```typescript
  // 正确的导入方式
  import presetWind3 from '@unocss/preset-wind3'
  import presetIcons from '@unocss/preset-icons'
  ```

### 4. 维护性
- 保持颜色变量的一致性
- 遵循命名约定
- 及时更新文档

### 5. 浏览器兼容性
- UnoCSS 提供良好的浏览器支持
- CSS 变量在现代浏览器中得到广泛支持
- 为旧版浏览器提供适当的回退方案

---

## 🔮 未来规划

### 短期目标
- [ ] 完善暗色模式实现
- [ ] 添加更多动画效果
- [ ] 优化移动端体验

### 中期目标
- [ ] 实现主题切换功能
- [ ] 添加更多颜色主题选项
- [ ] 优化动画性能

### 长期目标
- [ ] 支持自定义主题配置
- [ ] 添加主题导出功能
- [ ] 实现自适应主题

---

*本文档随项目更新而维护，最后更新时间：2025-12-16 (已更新 UnoCSS v0.66+ 配置)*